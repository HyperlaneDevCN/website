# `TypeCasts` 库

`TypeCasts` 库提供了简单高效的函数用于 `address` 和 `bytes32` 类型之间的转换。

当您需要在智能合约中使用 `bytes32` 类型,但仍需要将其转换回 `address` 类型(或反之)时,这个库非常有用。

## `TypeCasts` 库

```solidity file=<rootDir>/node_modules/@hyperlane-xyz/core/contracts/libs/TypeCasts.sol

```
